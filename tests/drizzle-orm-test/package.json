{
  "name": "drizzle-orm-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate": "drizzle-kit generate",
    "migrate": "node migrate.node.js",
    "migrate:bun": "bun migrate.bun.js",
    "start": "rm -f ./sqlite.db && npm run migrate && node index.node.js",
    "start:bun": "rm -f ./sqlite.db && npm run migrate:bun && bun index.bun.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "better-sqlite3": "^12.5.0",
    "drizzle-orm": "^0.45.1"
  },
  "devDependencies": {
    "bun-types": "^1.3.5",
    "drizzle-kit": "^0.31.8"
  }
}
